int fib(int n) {
  if (n < 2) 
    return n;
  else
    return fib(n - 2) + fib(n - 1);
}

void main() {
  int n;
  while (n < 42)
  {
    iprint(fib(n));
    n = n + 1;
  }
  return;
}
